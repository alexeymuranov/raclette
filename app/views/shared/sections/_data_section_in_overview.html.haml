-# Locals:
-#   models:
-#   name_method:
-#   i18n_controller_scope:
-#   index_path:
-#   new_path:
-#   show_path_method:

%section

  %h3
    = link_to list_all_pictogram(2) +                                     |
                ' ' + t('index.title', :scope => i18n_controller_scope ), |
              index_path,                                                 |
              :class => 'display resource'                                |

  - if models.is_a?(ActiveRecord::Relation) || models.is_a?(Enumerable)
    %ul.compact
      - models.each do |model|
        %li
          = link_to show_pictogram(2) + ' ' + model.public_send(name_method),
                    show_path_method.call(model),
                    :class => 'display resource'
  - else
    %p= models.to_s

  %footer
    = link_to add_pictogram(2) +                                         |
                ' ' + t('new.header', :scope => i18n_controller_scope ), |
              new_path,                                                  |
              :title => t('actions.create'),                             |
              :class => 'add resource'                                   |
