<%# Locals:
      models:
      attributes:
      column_types_o_hash:
-%>

<tbody>
  <% if models.blank? %>
    <tr>
      <%= content_tag :td, '∅', :colspan => attributes.size,
                                :class   => 'nothing' %>
      <td class="empty"></td>
      <td class="empty"></td>
    <tr>
  <% else %>
    <% models.each do |mod| %>
      <tr>

        <%= render 'shared/attributes_in_index_table',
                   :model               => mod,
                   :attributes          => attributes,
                   :column_types_o_hash => column_types_o_hash %>

        <td class="operation">
          <%= link_to edit_pictogram(2), [ :edit, mod ],
                      :title => t('actions.update'),
                      :class => 'modify resource' %>
        </td>

        <td class="operation">
          <%= link_to delete_pictogram(2),  mod,
                      :confirm => t('questions.are_you_sure?'),
                      :method  => :delete,
                      :title   => t('actions.delete'),
                      :class   => 'modify resource' %>
        </td>
      </tr>
    <% end %>
  <% end %>
</tbody>
