<%= form_for(@known_ip) do |f| %>
  <% if @known_ip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@known_ip.errors.count, "error") %> prohibited this known IP from being saved:</h2>

      <ul>
      <% @known_ip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ip, t('field_labels.ip') %>
    <%= f.text_field :ip, :disabled => !@known_ip.new_record? %>
  </div>
  <div class="field">
    <%= f.label :description, t('field_labels.description') %>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit t('buttons.save') %>
  </div>
<% end %>
