<% content_for :side_menu_list_items do %>
  <li>
    <%= link_to t('admin.known_i_ps.links.show_all'), admin_known_ips_path,
                :class => 'display' %>
  </li>
<% end %>

<h1><%= t '.header', :ip => @known_ip.ip %></h1>

<%= render 'form' %>

<footer>
  <%= link_to show_pictogram(2)+' '+t('.links.show'), @known_ip,
              :class => 'display' %>
  <%= link_to list_all_pictogram(2)+' '+t('admin.known_i_ps.links.show_all'), admin_known_ips_path,
              :class => 'display' %>
</footer>
