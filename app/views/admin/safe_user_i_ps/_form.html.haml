= form_tag({ :action => :update_all }, :method => :put) do |f|
  = field_set_tag do

    = render 'shared/forms/association_grid',
             :collection1            => @known_ips,
             :collection2            => @users,
             :association_attribute1 => :safe_users,
             :key1                   => :ip,
             :key2                   => :username,
             :description1           => :description,
             :description2           => :full_name,
             :id_array_name          => :safe_user_ids_for_known_ips

  .actions
    = submit_tag t('buttons.save')
