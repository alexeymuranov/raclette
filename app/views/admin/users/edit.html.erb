<h1><%= t '.header', :username => @user.username %></h1>

<%= render 'edit_form' %>

<%= link_to show_icon(2)+t('admin.users.edit.links.show'), @user %> |
<%= link_to list_all_icon(2)+t('admin.users.links.all'), admin_users_path %>
